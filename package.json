{
  "name": "confidential-payment",
  "version": "1.0.0",
  "description": "Confidential ERC20-like payment system using iExec Nox Protocol",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@iexec-nox/nox-protocol-contracts": "^0.1.0-beta.8",
    "ethers": "^6.0.0"
  }
}
